{
  "name": "juquinha",
  "version": "0.0.1",
  "description": "Tudo na mãozinha",
  "scripts": {
    "init": "npm run bootstrap && npx cross-env INIT=true node scripts/init",
    "bootstrap": "npx lerna bootstrap",
    "clean": "npx lerna clean -y && npx lerna exec -- rm -rf .serverless",
    "build": "npx lerna exec --scope sls-* sls package",
    "build:api": "npx lerna exec --scope sls-api-* sls package",
    "build:resources": "npx lerna exec --scope sls-resources-* sls package",
    "build:resources:api": "npx lerna exec --scope sls-resources-api sls package",
    "build:web": "npx lerna exec --scope sls-web npm run build",
    "deploy": "npx lerna exec --scope sls-* sls deploy",
    "deploy:fresh": "npm run clean && npm run bootstrap && npm run deploy",
    "deploy:api": "npx lerna exec --scope sls-api-* sls deploy",
    "deploy:resources": "npx lerna exec --scope sls-resources-* sls deploy",
    "deploy:resources:api": "npx lerna exec --scope sls-resources-api sls deploy",
    "deploy:web": "npx lerna exec --scope sls-web sls deploy",
    "remove": "npm run remove:api && npm run remove:resources && npm run remove:web",
    "remove:api": "npx lerna exec --scope sls-api-* sls remove",
    "remove:resources:api": "npx lerna exec --scope sls-resources-api sls remove",
    "remove:resources": "npx lerna exec --scope sls-resources-* sls remove",
    "remove:web": "npx lerna exec --scope sls-web sls remove",
    "docker:build": "docker build . -t juquinha:latest",
    "docker:run": "docker run --rm juquinha:latest",
    "docker:deploy": "docker run --rm juquinha:latest npm run deploy",
    "web:dev": "(cd modules/web && npm run dev)",
    "test:integration": "npx jest \"^(.*/)*(.*\\.)*(integration)\\.(spec)\\.js$\""
  },
  "dependencies": {
    "config": "^0.0.1",
    "lib": "^0.0.1"
  },
  "devDependencies": {
    "@babel/core": "^7.16.0",
    "@babel/preset-env": "^7.16.4",
    "@rodrigogs/serverless-finch": "^3.0.0",
    "chalk": "^4.1.2",
    "eslint": "^7.32.0",
    "eslint-config-standard": "^16.0.3",
    "eslint-plugin-import": "^2.25.2",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^5.1.1",
    "faker": "^5.5.3",
    "inquirer": "^8.2.0",
    "jest": "^27.4.5",
    "loading-cli": "^1.1.0",
    "prettier": "^2.4.1",
    "serverless": "^2.65.0",
    "serverless-bundle": "^4.4.0",
    "serverless-offline": "^8.2.0",
    "serverless-scriptable-plugin": "^1.2.2"
  },
  "engines": {
    "node": ">=14.18.2"
  }
}
