{
  "name": "juquinha",
  "version": "0.0.1",
  "description": "Tudo na mãozinha",
  "scripts": {
    "init": "npx pnpm run bootstrap && npx cross-env INIT=true node scripts/init",
    "bootstrap": "npx pnpm recursive install",
    "clean": "npx pnpm -r exec -- rm -rf node_modules && npx pnpm -r exec -- rm -rf .serverless",
    "build": "npx pnpm -r --filter sls-* exec -- sls package",
    "build:api": "npx pnpm -r --filter sls-api-* exec -- sls package",
    "build:resources": "npx pnpm -r --filter sls-resources-* exec -- sls package",
    "build:resources:api": "npx pnpm -r --filter sls-resources-api exec -- sls package",
    "build:web": "npx pnpm -r --filter sls-web exec -- npx pnpm run build",
    "deploy": "npx pnpm -r --filter sls-* exec -- sls deploy",
    "deploy:fresh": "npx pnpm run clean && npx pnpm run bootstrap && npx pnpm run deploy",
    "deploy:api": "npx pnpm -r --filter sls-api-* exec -- sls deploy",
    "deploy:resources": "npx pnpm -r --filter sls-resources-* exec -- sls deploy",
    "deploy:resources:api": "npx pnpm -r --filter sls-resources-api exec -- sls deploy",
    "deploy:web": "npx pnpm -r --filter sls-web exec -- sls deploy",
    "remove": "npx pnpm run remove:api && npx pnpm run remove:resources && npx pnpm run remove:web",
    "remove:api": "npx pnpm -r --filter sls-api-* exec -- sls remove",
    "remove:resources:api": "npx pnpm -r --filter sls-resources-api exec -- sls remove",
    "remove:resources": "npx pnpm -r --filter sls-resources-* exec -- sls remove",
    "remove:web": "npx pnpm -r --filter sls-web exec -- sls remove",
    "docker:build": "docker build . -t juquinha:latest",
    "docker:run": "docker run --rm juquinha:latest",
    "docker:deploy": "docker run --rm juquinha:latest npx pnpm run deploy",
    "web:dev": "(cd modules/web && npx pnpm run dev)",
    "test:integration": "npx jest \"^(.*/)*(.*\\.)*(integration)\\.(spec)\\.js$\""
  },
  "dependencies": {
    "config": "^0.0.1",
    "lib": "^0.0.1"
  },
  "devDependencies": {
    "@babel/core": "^7.16.0",
    "@babel/preset-env": "^7.16.4",
    "@rodrigogs/serverless-finch": "^3.0.0",
    "chalk": "^4.1.2",
    "eslint": "^7.32.0",
    "eslint-config-standard": "^16.0.3",
    "eslint-plugin-import": "^2.25.2",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^5.1.1",
    "faker": "^5.5.3",
    "inquirer": "^8.2.0",
    "jest": "^27.4.5",
    "loading-cli": "^1.1.0",
    "prettier": "^2.4.1",
    "serverless": "^2.65.0",
    "serverless-bundle": "^4.4.0",
    "serverless-offline": "^8.2.0",
    "serverless-scriptable-plugin": "^1.2.2",
    "source-map-loader": "^3.0.0",
    "strip-ansi": "6"
  },
  "engines": {
    "node": ">=14.18.2",
    "pnpm": ">=6.0.0"
  }
}
