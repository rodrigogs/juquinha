/* eslint-disable import/no-commonjs */
const bucketExists = require('./bucket-exists')
const bucketExistsAndAccessible = require('./bucket-exists-and-accessible')

module.exports = {
  bucketExists,
  bucketExistsAndAccessible,
}
